---
export interface Props {
  url: string;
  label: string;
}

const { url, label } = Astro.props;
---

<a
  href={url}
  title={label}
  aria-label={label}
  class="transition-all underline-offset-2 hover:underline active:underline"
  >{label}
</a>
